name: ifood_engenharia_de_software
description: Versão simplificado do Ifood feita em Flutter para a disciplina de Engenharia de Software 2021.2

publish_to: 'none'

version: 1.0.0+1

analyzer:
  enable-experiment:
    - non-nullable

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  get: ^4.3.8
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.10.0
  firebase_analytics: ^8.3.4
  firebase_auth: ^3.2.0
  cloud_firestore: ^3.1.0
  flutter_barcode_scanner: ^2.0.0
  
dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
